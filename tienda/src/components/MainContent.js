import React from 'react';

function MainContent() {
  return (
    <main>
      {/* Aquí puedes incluir más contenido como secciones de productos, etc. */}
      <div id="contenedor" className="contenedor">
        {/* Ejemplo de contenido */}
      </div>
    </main>
  );
}

export default MainContent;
